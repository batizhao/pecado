#if(!$columns.isEmpty())
ALTER TABLE ${tableName}
  ADD(
#foreach($column in $columns)
   `${column.name}` #if(${column.type}=="collection"||${column.type}=="file")json#else${column.type}#end#if(${column.length}>0)(${column.length}#if(${column.type}=="decimal"),${column.decimal}#end)#end#if(${column.required}) NOT NULL#end#if(${column.defaultValue}) DEFAULT '${column.defaultValue}'#end#if(${column.increment}) AUTO_INCREMENT#end COMMENT '${column.comment}'#if( $foreach.hasNext ),#end
#end);
#end
